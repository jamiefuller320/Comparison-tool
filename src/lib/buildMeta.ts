/** Deploy / campaign metadata for soft-launch prompts. */

export const APP_VERSION = "0.1.0";

/**
 * Bump this when a significant build ships and returning visitors should see
 * the under-development feedback prompt again (new campaign id).
 */
export const FEEDBACK_CAMPAIGN_ID = "soft-launch-2026-08";
