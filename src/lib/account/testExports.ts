/** Node test entry — re-exports account helpers without React. */
export {
  ACCOUNT_PROMPT_MIN_SCHOOLS,
  buildShortlistSnapshot,
  createLocalAccountBackend,
  isValidEmail,
  normalizeEmail,
} from "@/lib/account/index";
